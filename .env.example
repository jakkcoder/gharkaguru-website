# Local dev — copy to .env.local and adjust as needed

# Leave empty to use MSW mocks (no backend required)
# VITE_API_BASE_URL=

# Point at local gateway:
# VITE_API_BASE_URL=http://localhost:8080/v1

# Production bucket build uses .env.production automatically.
